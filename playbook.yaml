---
- name: ansible-vaults
  hosts: local
  connection: local

  vars_files:
    - ss.yaml

  tasks:
    - name: View the data
      ansible.builtin.debug:
        msg: "ADMIN: {{ Admin }}, PASSWORD: {{ Password }}"