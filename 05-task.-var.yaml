- name: variables
  hosts: web
  vars:
    COURSE: DEVOPS
    TARINER: PERAIAH
    DURATION: 10HRS

  tasks:
  - name: print the data
    vars:
      TARINER: peraiah
    ansible.builtin.debug:
      msg: "the course is: {{COURSE}}, tariner: {{TARINER}}, duration: {{DURATION}}"