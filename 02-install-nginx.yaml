- name: install and start 
  hosts: web
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: latest

  - name: start nginx
    ansible.builtin.package:
      name: nginx
      state: started
      enabled: yes